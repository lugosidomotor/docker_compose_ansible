all:
  children:
    east:
      hosts:
        172.16.100.140:  # alpha
        172.16.100.141:  # bravo
    west:
      hosts:
        172.16.100.142:  # charlie

vars:
  ansible_user: ansible
  ansible_ssh_private_key_file: id_rsa
  timezone: Europe/Berlin
  apt_mirror: http://ftp.hosteurope.de/mirror/archive.ubuntu.com/ubuntu